'use client'

import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import { Bell, CalendarDays, Check, ChevronRight, CircleDollarSign, HardHat, HeartPulse, Home, Inbox, ListChecks, Mail, MapPin, Mic, MoreHorizontal, Plus, Search, ShieldCheck, Sparkles, StickyNote, Target, Users, Utensils, WalletCards, X, Image as ImageIcon, Clock3 } from 'lucide-react'
import { readReminderCache, writeReminderCache } from '../lib/reminder-client-cache'

type Section='Home'|'Inbox'|'Tasks'|'Calendar'|'Family'|'Projects'|'Health'|'Finance'|'Journal'
type Task={id:number;title:string;meta:string;priority:'High'|'Medium'|'Low';done:boolean}
type Note={id:number;text:string;created:string}
type HomeReminder={id:string;title:string;notes?:string;date:string;time:string;source?:'text'|'voice'|'image';imageName?:string;posterUrl?:string;createdBy?:string}
const initialTasks:Task[]=[{id:1,title:'Review India house contractor milestone',meta:'Construction · India House',priority:'High',done:false},{id:2,title:'Review Ravi OS deployment',meta:'Ravi OS · Today',priority:'Medium',done:false},{id:3,title:'Review weekly personal commitments',meta:'Planning · Weekly',priority:'Low',done:false}]
const inboxes=[{name:'Personal',address:'ravi14feb@gmail.com',unread:1},{name:'Webfit News',address:'webfitnews@gmail.com',unread:1},{name:'Career',address:'Professional mailbox',unread:0}]
function greeting(d:Date){const h=Number(new Intl.DateTimeFormat('en-NZ',{timeZone:'Pacific/Auckland',hour:'2-digit',hourCycle:'h23'}).format(d));return h<12?'Good morning':h<17?'Good afternoon':'Good evening'}
function weekday(d:Date){return new Intl.DateTimeFormat('en-NZ',{timeZone:'Pacific/Auckland',weekday:'long'}).format(d)}
function dietFor(day:string){return ['Monday','Wednesday','Friday'].includes(day)?'Non-veg allowed':'Vegetarian day'}
function reminderTime(r:HomeReminder){return new Date(`${r.date}T${r.time||'23:59'}:00`).getTime()}
function reminderUrgency(r:HomeReminder,now:Date){const target=reminderTime(r),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()).getTime(),targetDay=new Date(`${r.date}T00:00:00`).getTime(),dayDiff=Math.round((targetDay-today)/86400000);if(target<now.getTime()||dayDiff===0)return'urgent';if(dayDiff===1)return'tomorrow';if(dayDiff===2)return'soon';return'later'}
function reminderWhen(r:HomeReminder,now:Date){const diff=reminderTime(r)-now.getTime();if(diff<0){const mins=Math.max(1,Math.floor(Math.abs(diff)/60000));if(mins<60)return`Overdue by ${mins}m`;return`Overdue by ${Math.floor(mins/60)}h`}const mins=Math.floor(diff/60000);if(mins<60)return`${Math.max(1,mins)}m left`;if(mins<1440)return`${Math.floor(mins/60)}h ${mins%60}m left`;const days=Math.floor(mins/1440);return`${days}d ${Math.floor((mins%1440)/60)}h left`}
function reminderDayLabel(r:HomeReminder,now:Date){const today=new Date(now.getFullYear(),now.getMonth(),now.getDate()).getTime(),targetDay=new Date(`${r.date}T00:00:00`).getTime(),d=Math.round((targetDay-today)/86400000);if(d===0)return'TODAY';if(d===1)return'TOMORROW';if(d===2)return'DAY AFTER';if(d<0)return'OVERDUE';return new Date(`${r.date}T12:00:00`).toLocaleDateString('en-NZ',{day:'numeric',month:'short'})}

export default function Page(){
 const[now,setNow]=useState(new Date());const[active,setActive]=useState<Section>('Home');const[tasks,setTasks]=useState<Task[]>(initialTasks);const[notes,setNotes]=useState<Note[]>([]);const[reminders,setReminders]=useState<HomeReminder[]>([]);const[sheet,setSheet]=useState<'actions'|'notifications'|'task'|'note'|'more'|null>(null);const[taskTitle,setTaskTitle]=useState('');const[noteText,setNoteText]=useState('');const[location,setLocation]=useState('Auckland')
 useEffect(()=>{const cached=readReminderCache<HomeReminder>();if(cached.length)setReminders(cached);const load=async()=>{try{const r=await fetch('/api/reminders');if(r.ok){const d=await r.json(),rows=d.reminders||[];setReminders(rows);writeReminderCache(rows)}}catch{}};const timer=setInterval(()=>{setNow(new Date());load()},60000);const st=localStorage.getItem('ravi-os-tasks-v5'),sn=localStorage.getItem('ravi-os-notes-v1');if(st)setTasks(JSON.parse(st));if(sn)setNotes(JSON.parse(sn));load();return()=>clearInterval(timer)},[])
 useEffect(()=>localStorage.setItem('ravi-os-tasks-v5',JSON.stringify(tasks)),[tasks]);useEffect(()=>localStorage.setItem('ravi-os-notes-v1',JSON.stringify(notes)),[notes])
 const day=useMemo(()=>weekday(now),[now]),openTasks=tasks.filter(t=>!t.done);const go=(s:Section)=>{setActive(s);setSheet(null);window.scrollTo({top:0,behavior:'smooth'})};const toggleTask=(id:number)=>setTasks(p=>p.map(t=>t.id===id?{...t,done:!t.done}:t));const addTask=()=>{const title=taskTitle.trim();if(!title)return;setTasks(p=>[{id:Date.now(),title,meta:'Personal · Added now',priority:'Medium',done:false},...p]);setTaskTitle('');setSheet(null);setActive('Tasks')};const addNote=()=>{const text=noteText.trim();if(!text)return;setNotes(p=>[{id:Date.now(),text,created:'Just now'},...p]);setNoteText('');setSheet(null);setActive('Journal')};const enableLocation=()=>navigator.geolocation?.getCurrentPosition(()=>setLocation('Live location enabled'),()=>setLocation('Auckland'))
 return <main className="appShell"><aside className="sidebar desktopOnly"><button className="brand" onClick={()=>go('Home')}><span className="brandMark">RG</span><span><b>Ravi OS</b><small>Personal assistant</small></span></button><nav className="sideNav"><button className={active==='Home'?'active':''} onClick={()=>go('Home')}><Home/>Home</button><button className={active==='Inbox'?'active':''} onClick={()=>go('Inbox')}><Inbox/>Inbox <i>2</i></button><button onClick={()=>go('Tasks')}><ListChecks/>Tasks</button><button onClick={()=>go('Calendar')}><CalendarDays/>Calendar</button><Link href="/reminders"><Bell/>Reminders</Link><Link href="/construction"><HardHat/>Construction</Link><button onClick={()=>go('Family')}><Users/>Family</button><button onClick={()=>go('Health')}><HeartPulse/>Health</button><button onClick={()=>go('Finance')}><WalletCards/>Finance</button></nav><div className="sidebarFooter"><ShieldCheck/><span><b>Private workspace</b><small>PIN protected</small></span></div></aside><section className="content"><header className="mobileHeader"><div><p>RAVI OS</p><h1>{active==='Home'?`${greeting(now)}, Ravi.`:active}</h1></div><div className="headerIcons"><button aria-label="Search"><Search/></button><button aria-label="Notifications" onClick={()=>setSheet('notifications')}><Bell/></button></div></header>{active==='Home'&&<HomeView now={now} day={day} location={location} diet={dietFor(day)} openTasks={openTasks.length} reminders={reminders} go={go} setSheet={setSheet} enableLocation={enableLocation}/>} {active==='Inbox'&&<InboxView/>}{active==='Tasks'&&<TasksView tasks={tasks} toggleTask={toggleTask} onAdd={()=>setSheet('task')}/>} {active==='Calendar'&&<CalendarView/>}{active==='Family'&&<SimpleView icon={<Users/>} title="Family" text="Family appointments, school, PTM, doctor visits and shared responsibilities."/>}{active==='Projects'&&<SimpleView icon={<Target/>} title="Projects" text="Personal and professional projects, separated from daily tasks."/>}{active==='Health'&&<SimpleView icon={<HeartPulse/>} title="Health" text="Health signals and Apple Health integration will live here."/>}{active==='Finance'&&<SimpleView icon={<CircleDollarSign/>} title="Finance" text="Protected bills, accounts, payments and financial reminders."/>}{active==='Journal'&&<JournalView notes={notes}/>}</section><nav className="bottomNav"><button className={active==='Home'?'active':''} onClick={()=>go('Home')}><Home/><span>Home</span></button><button onClick={()=>go('Tasks')}><ListChecks/><span>Tasks</span></button><button className="primaryAdd" onClick={()=>setSheet('actions')}><Plus/></button><button onClick={()=>go('Calendar')}><CalendarDays/><span>Calendar</span></button><button onClick={()=>setSheet('more')}><MoreHorizontal/><span>More</span></button></nav>{sheet&&<div className="sheetBackdrop" onClick={()=>setSheet(null)}><div className="bottomSheet" onClick={e=>e.stopPropagation()}><div className="sheetHandle"/><button className="sheetClose" onClick={()=>setSheet(null)}><X/></button>{sheet==='actions'&&<ActionSheet onTask={()=>setSheet('task')} onNote={()=>setSheet('note')}/>} {sheet==='notifications'&&<NotificationSheet go={go}/>} {sheet==='task'&&<CreateTask title={taskTitle} setTitle={setTaskTitle} save={addTask}/>} {sheet==='note'&&<CreateNote text={noteText} setText={setNoteText} save={addNote}/>} {sheet==='more'&&<MoreSheet go={go}/>}</div></div>}</main>
}
function HomeView({now,day,location,diet,openTasks,reminders,go,setSheet,enableLocation}:{now:Date;day:string;location:string;diet:string;openTasks:number;reminders:HomeReminder[];go:(s:Section)=>void;setSheet:(s:any)=>void;enableLocation:()=>void}){const auckland=new Intl.DateTimeFormat('en-NZ',{timeZone:'Pacific/Auckland',hour:'numeric',minute:'2-digit'}).format(now),india=new Intl.DateTimeFormat('en-NZ',{timeZone:'Asia/Kolkata',hour:'numeric',minute:'2-digit'}).format(now),ordered=[...reminders].sort((a,b)=>reminderTime(a)-reminderTime(b)),visible=ordered.slice(0,4);return <><section className="todayStrip"><button onClick={enableLocation}><MapPin/><span>{location}</span></button><span>{day}</span><span><b>Auckland</b> {auckland}</span><span><b>India</b> {india}</span><span className="dietChip"><Utensils/>{diet}</span></section><section className="reminderHero"><div className="reminderHeroHead"><div><p>NEXT UP</p><h2><Bell/> Your reminders</h2><small>Nearest date and time always comes first.</small></div><Link href="/reminders">View all <ChevronRight/></Link></div>{visible.length===0?<Link className="reminderEmpty" href="/reminders"><Bell/><span><b>No reminders yet</b><small>Tap to add one from a poster or type it.</small></span><Plus/></Link>:<div className="homeReminderList">{visible.map(r=><Link href={`/reminders/${r.id}`} className={`homeReminder ${reminderUrgency(r,now)}`} key={r.id}><span className="homeReminderDate"><b>{reminderDayLabel(r,now)}</b><strong>{r.time}</strong></span><span className="homeReminderBody"><b>{r.title}</b><small>{new Date(`${r.date}T12:00:00`).toLocaleDateString('en-NZ',{weekday:'short',day:'numeric',month:'short'})} · {r.time}</small></span><span className="homeReminderCountdown"><Clock3/><b>{reminderWhen(r,now)}</b></span></Link>)}</div>}<Link className="addReminderNow" href="/reminders"><Plus/> Add reminder</Link></section><section className="quickCapture"><div><Sparkles/><span><b>What do you need to remember?</b><small>Add a reminder, task or note.</small></span></div><button onClick={()=>setSheet('actions')}><Plus/>Add something</button></section><section className="sectionBlock"><div className="sectionTitle"><div><p>RIGHT NOW</p><h2>Needs your attention</h2></div><button onClick={()=>setSheet('notifications')}>View all</button></div><div className="notificationList"><Link href="/construction"><span className="notifIcon red"><HardHat/></span><span><b>Construction workspace</b><small>Review milestones, payments and actions</small></span><ChevronRight/></Link></div></section><section className="homeCards"><button className="summaryCard" onClick={()=>go('Tasks')}><span><ListChecks/></span><div><small>OPEN TASKS</small><strong>{openTasks}</strong><p>Tap to review and add tasks</p></div><ChevronRight/></button><button className="summaryCard" onClick={()=>go('Calendar')}><span><CalendarDays/></span><div><small>NEXT</small><strong>Calendar</strong><p>Appointments and commitments</p></div><ChevronRight/></button></section></>}
function ActionSheet({onTask,onNote}:{onTask:()=>void;onNote:()=>void}){return <><div className="sheetTitle"><p>QUICK ADD</p><h2>What are you adding?</h2></div><div className="actionGrid"><Link href="/reminders"><span className="actionIcon green"><Bell/></span><b>Reminder</b><small>Poster or quick entry</small></Link><button onClick={onTask}><span className="actionIcon blue"><ListChecks/></span><b>Task</b><small>Something to do</small></button><Link href="/reminders"><span className="actionIcon amber"><ImageIcon/></span><b>Event poster</b><small>Choose an image</small></Link><button onClick={onNote}><span className="actionIcon grey"><StickyNote/></span><b>Note</b><small>Save a thought</small></button><Link href="/construction"><span className="actionIcon red"><HardHat/></span><b>Construction</b><small>House update</small></Link></div></>}
function NotificationSheet({go}:{go:(s:Section)=>void}){return <><div className="sheetTitle"><p>NOTIFICATIONS</p><h2>Things to know</h2></div><div className="sheetList"><Link href="/construction"><HardHat/><span><b>Construction workspace</b><small>Review project actions</small></span><ChevronRight/></Link><Link href="/reminders"><Bell/><span><b>Reminder Centre</b><small>Create or review reminders</small></span><ChevronRight/></Link></div></>}
function CreateTask({title,setTitle,save}:{title:string;setTitle:(v:string)=>void;save:()=>void}){return <div className="formSheet"><div className="sheetTitle"><p>NEW TASK</p><h2>Add a task</h2></div><label>What needs to be done?<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} onKeyDown={e=>e.key==='Enter'&&save()} placeholder="e.g. Call contractor tomorrow"/></label><button className="primaryButton" onClick={save}>Add task</button></div>}
function CreateNote({text,setText,save}:{text:string;setText:(v:string)=>void;save:()=>void}){return <div className="formSheet"><div className="sheetTitle"><p>NEW NOTE</p><h2>Capture a thought</h2></div><label>Note<textarea autoFocus value={text} onChange={e=>setText(e.target.value)} placeholder="Write anything…"/></label><button className="primaryButton" onClick={save}>Save note</button></div>}
function MoreSheet({go}:{go:(s:Section)=>void}){return <><div className="sheetTitle"><p>RAVI OS</p><h2>More</h2></div><div className="sheetList"><button onClick={()=>go('Inbox')}><Inbox/><span><b>Inbox</b><small>Connected email accounts</small></span><ChevronRight/></button><button onClick={()=>go('Family')}><Users/><span><b>Family</b><small>Shared commitments</small></span><ChevronRight/></button><button onClick={()=>go('Health')}><HeartPulse/><span><b>Health</b><small>Health and nutrition</small></span><ChevronRight/></button><button onClick={()=>go('Finance')}><WalletCards/><span><b>Finance</b><small>Protected financial area</small></span><ChevronRight/></button><button onClick={()=>go('Journal')}><StickyNote/><span><b>Journal</b><small>Notes and reflection</small></span><ChevronRight/></button></div></>}
function InboxView(){return <section className="modulePage"><div className="moduleHeading"><p>PERSONAL SECRETARY</p><h2>Inbox</h2><span>Email intelligence will classify Action, Waiting, FYI and Noise.</span></div><div className="inboxList">{inboxes.map(i=><div className="inboxRow" key={i.name}><span className="notifIcon blue"><Mail/></span><div><b>{i.name}</b><small>{i.address}</small></div><strong>{i.unread}</strong></div>)}</div></section>}
function TasksView({tasks,toggleTask,onAdd}:{tasks:Task[];toggleTask:(id:number)=>void;onAdd:()=>void}){return <section className="modulePage"><div className="moduleHeading row"><div><p>TASKS</p><h2>What needs doing?</h2></div><button onClick={onAdd}><Plus/>Add task</button></div><div className="taskList">{tasks.map(t=><button className={t.done?'done':''} onClick={()=>toggleTask(t.id)} key={t.id}><span className="checkCircle">{t.done&&<Check/>}</span><span><b>{t.title}</b><small>{t.meta}</small></span><i className={`priority ${t.priority.toLowerCase()}`}>{t.priority}</i></button>)}</div></section>}
function CalendarView(){return <SimpleView icon={<CalendarDays/>} title="Calendar" text="Your unified timeline of reminders, tasks and commitments."/>}
function JournalView({notes}:{notes:Note[]}){return <section className="modulePage"><div className="moduleHeading"><p>JOURNAL</p><h2>Notes & reflection</h2><span>Private notes. Persistent journal is available from the Journal workspace.</span></div>{notes.length===0?<div className="emptyState"><StickyNote/><b>No notes yet</b></div>:<div className="noteList">{notes.map(n=><div key={n.id}><b>{n.text}</b><small>{n.created}</small></div>)}</div>}</section>}
function SimpleView({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <section className="simpleView"><div className="simpleIcon">{icon}</div><p>RAVI OS</p><h2>{title}</h2><span>{text}</span></section>}
